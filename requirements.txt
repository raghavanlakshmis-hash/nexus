# Direct dependencies only. Versions are left open (with a few safe bounds) so pip
# resolves wheels for whatever Python the host runs — this avoids the build failures
# the old fully-pinned, Python-3.11-frozen file caused on newer cloud Python.
# Transitive deps (numpy, pandas, pyarrow, altair, protobuf, tornado, gitpython, ...)
# are pulled automatically by streamlit and the SDKs below.

# Web UI
streamlit

# LLM SDKs
anthropic
openai

# Agent orchestration — pin to the 1.x line that matches local (StateGraph lives in
# langgraph.graph here). langgraph pulls its own compatible langchain-core/checkpoint.
langgraph>=1.2.5,<2

# Vector store (v5 API: `from pinecone import Pinecone`)
pinecone-client>=5,<6

# PDF parsing (intake agent)
PyMuPDF

# HTTP — OpenFDA interaction checks + ElevenLabs STT REST calls
requests

# Environment variables
python-dotenv

# Voice check-in (requires ffmpeg, installed via packages.txt)
streamlit-audiorecorder
pydub

# SMS notifications (escalation agent)
twilio
